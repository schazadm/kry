/* Aufruf mit read("Script_Example"). Falls sich das File nicht im gleichen Ordner befindet wie gp.exe, braucht es zusätzlich die Angabe des Pfades. */
/* Hinweis: File muss Endung .gp haben. */
{
u = 1;
v = 2;
w = u + v;
print("Resultat der Addition=" w);
}