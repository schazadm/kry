/* Aufruf mit den Beispiel-Werten x = 3, y = 4: 
read("Function_Example")
Function_Example(3,4)
Falls sich das File nicht im gleichen Ordner befindet wie gp.exe, braucht es zusätzlich die Angabe des Pfades. */

/*Hinweis: Der File-Name muss nicht zwingend mit dem Funktionsnamen übereinstimmen. */

fun1(x,y) =
{
d = x + y;
return (d)
}

fun2(a,b) =
{
l = a*b + 10;
return (l);
}